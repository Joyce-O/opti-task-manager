"use strict";
// import {Router} from 'express';
// import {authorize} from '@src/middleware/authorize';
// import validateRequest from '@src/middleware/validateRequest';
// import {createTaskSchema, taskIdSchema} from '@src/db/schema/task';
// const router = Router();
// router.post(
//   '/tasks',
//   authorize,
//   validateRequest(createTaskSchema),
//   loginHandler
// );
// router.get('/tasks', authorize, createUserHandler);
// router.get(
//   '/tasks/:taskId',
//   authorize,
//   validateRequest(taskIdSchema),
//   createUserHandler
// );
// router.put('/tasks', authorize, createUserHandler);
// router.delete(
//   '/tasks/taskId:',
//   authorize,
//   validateRequest(taskIdSchema),
//   createUserHandler
// );
